# --- Frontend configuration ---
# URL del endpoint local que reenvía correos usando Resend.
VITE_MAIL_API_URL="/api/send-mail"
# URL del endpoint local que expone el chat con Gemini.
VITE_CHAT_API_URL="/api/chat"

# --- Servidor de correo y chat (server/index.js) ---
# Copia estos valores al mismo archivo .env o a .env.mail antes de ejecutar `npm run server`.
# Puedes obtener la API key desde https://resend.com/api-keys
RESEND_API_KEY=""

# Destinatarios separados por coma que recibirán los correos del formulario y las transcripciones del chat.
RESEND_RECIPIENTS="juanchacon@rv2ven.com,juanchacon0298@gmail.com"

# Valores opcionales para personalizar el remitente mostrado en los correos.
RESEND_FORM_FROM="RV2 Web <notificaciones@tudominio.com>"
RESEND_CHAT_FROM="RV2 Chat <notificaciones@tudominio.com>"

# Clave de Gemini para generar las respuestas del chatbot.
# Crea una en https://ai.google.dev/gemini-api/docs/api-key
GEMINI_API_KEY=""

# Puerto donde escuchará el servidor local.
PORT=8787
