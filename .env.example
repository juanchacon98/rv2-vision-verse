# --- Frontend configuration ---
# Supabase se sigue utilizando exclusivamente para la función de chat IA.
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-public-anon-key"

# URL del endpoint local que reenvía correos usando Resend.
VITE_MAIL_API_URL="http://localhost:8787/api/send-mail"

# --- Servidor de correo (server/index.js) ---
# Copia estos valores al mismo archivo .env o a .env.mail antes de ejecutar `npm run server`.
# Puedes obtener la API key desde https://resend.com/api-keys
RESEND_API_KEY=""

# Destinatarios separados por coma que recibirán los correos.
RESEND_RECIPIENTS="correo1@ejemplo.com,correo2@ejemplo.com"

# Valores opcionales para personalizar el remitente mostrado en los correos.
RESEND_FORM_FROM="RV2 Web <notificaciones@tudominio.com>"
RESEND_CHAT_FROM="RV2 Chat <notificaciones@tudominio.com>"

# Puerto donde escuchará el servidor local de correos.
PORT=8787
